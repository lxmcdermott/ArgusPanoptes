# Argus Panoptes v1 - core runtime dependencies
# Python 3.11+ recommended (tested on 3.13)

# --- Numerics & signal processing ---
numpy>=1.26
scipy>=1.11

# --- Data / storage ---
pandas>=2.1
pyarrow>=14.0

# --- Config / validation ---
pydantic>=2.5
PyYAML>=6.0

# --- Visualization (validation & experiments) ---
matplotlib>=3.8

# --- Progress / CLI utilities ---
tqdm>=4.66

# --- Testing ---
pytest>=7.4

# ---------------------------------------------------------------------------
# ML baseline (Day 2) is provided via the "ml" optional-dependency extra to keep
# the core install lean. Install it with:
#     pip install -e ".[ml]"
# which pulls in:
#   scikit-learn>=1.5, xgboost>=2.1, joblib>=1.4   (see pyproject.toml [ml])
#
# Deep-learning + ONNX edge export (Day 3) is the "dl" extra:
#     pip install -e ".[dl]"
# which pulls in:
#   torch>=2.2, onnx>=1.16, onnxruntime>=1.18      (see pyproject.toml [dl])
# The DL training script also uses the ml extra for its XGBoost comparison:
#     pip install -e ".[ml,dl]"
#
# Still deferred to later days of the execution plan:
#   App:       fastapi, uvicorn, streamlit, plotly   (Days 4-5)
# ---------------------------------------------------------------------------
